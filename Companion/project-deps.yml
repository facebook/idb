# @generated by the idb project.yml generator -- do not edit by hand.
#
# The products of the generated FBSimulatorControl project that each tool in
# Companion/project.yml links, derived from the tool's Buck dependency
# closure. Companion/project.yml includes this file, and XcodeGen merges
# these dependency lists into the tool targets it defines.
#
# The tools live in a separate Xcode project, so they cannot depend on those
# targets directly and link their Release products by path instead. Static
# libraries do not link transitively, which is why the whole closure is
# listed rather than the tool's direct dependencies.

targets:
  idb_companion:
    dependencies:
      - framework: ../Build/Products/Release/libCompanionLib.a
        embed: false
      - framework: ../Build/Products/Release/libCompanionUtilities.a
        embed: false
      - framework: ../Build/Products/Release/FBControlCore.framework
        embed: false
      - framework: ../Build/Products/Release/libFBDeviceControl.a
        embed: false
      - framework: ../Build/Products/Release/libFBSimulatorControl.a
        embed: false
      - framework: ../Build/Products/Release/libSimulatorXCTest.a
        embed: false
      - framework: ../Build/Products/Release/XCTestBootstrap.framework
        embed: false
  idb-repl:
    dependencies:
      - framework: ../Build/Products/Release/libCompanionUtilities.a
        embed: false
