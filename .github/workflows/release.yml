name: Release
on:
  push:
    tags:
      - 'v*'
  # Allows the build and packaging legs to be exercised without cutting a tag;
  # the release-creation steps only run on tag pushes.
  workflow_dispatch:

permissions:
  contents: write

jobs:
  # The single owner of `gh release create`: with both build jobs creating the
  # release concurrently, whichever won the race decided the release's title
  # and notes. Skipped on workflow_dispatch (no tag to release); the build jobs
  # tolerate that skip explicitly below.
  create-release:
    name: Create the GitHub release
    runs-on: ubuntu-latest
    timeout-minutes: 10
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Create release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Tolerate re-runs of the same tag: only create the release if it
          # does not already exist, and never mask a genuine creation failure.
          if gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" > /dev/null 2>&1; then
            echo "Release $GITHUB_REF_NAME already exists, leaving it as-is"
          else
            gh release create "$GITHUB_REF_NAME" --prerelease --title "$GITHUB_REF_NAME" --generate-notes --repo "$GITHUB_REPOSITORY"
          fi

  companion:
    name: Build and publish idb_companion
    runs-on: macos-26
    timeout-minutes: 120
    # `!cancelled()` suppresses the implicit success() so this still runs when
    # create-release is skipped (workflow_dispatch), while a failed or
    # cancelled create-release still blocks the upload legs.
    needs: [create-release]
    if: ${{ !cancelled() && (needs.create-release.result == 'success' || needs.create-release.result == 'skipped') }}
    env:
      XCODEGEN_STRIP_XATTRS: 'false'
    steps:
      - uses: actions/checkout@v4
      - name: Install tools
        run: brew install xcodegen protobuf swift-protobuf
      - name: Cache grpc-swift plugin
        uses: actions/cache@v4
        with:
          path: Build/grpc-swift
          key: grpc-swift-1.23.1-${{ runner.os }}
      - name: Build everything and assemble the distribution
        run: ./build.sh build all
      - name: Package
        id: package
        run: |
          tar -C Build/Distribution -czf idb-companion.universal.tar.gz .
          shasum -a 256 idb-companion.universal.tar.gz | tee idb-companion.universal.tar.gz.sha256
          echo "sha256=$(cut -d' ' -f1 idb-companion.universal.tar.gz.sha256)" >> "$GITHUB_OUTPUT"
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: idb-companion
          path: idb-companion.universal.tar.gz*
      - name: Upload to release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "$GITHUB_REF_NAME" idb-companion.universal.tar.gz idb-companion.universal.tar.gz.sha256 --clobber --repo "$GITHUB_REPOSITORY"
      - name: Homebrew formula update instructions
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          {
            echo '## Update the homebrew formula'
            echo 'In the `facebook/fb` tap, update `idb.rb` (it installs the companion and the python CLI together) for this release:'
            echo ''
            echo '- Point the companion `url`/`sha256` at this tarball. The digest below is also published as the `idb-companion.universal.tar.gz.sha256` release asset:'
            echo '```ruby'
            echo "url \"https://github.com/$GITHUB_REPOSITORY/releases/download/$GITHUB_REF_NAME/idb-companion.universal.tar.gz\""
            echo "sha256 \"${{ steps.package.outputs.sha256 }}\""
            echo '```'
            echo '- Update the explicit `version` stanza to match the tag: Homebrew mis-parses prerelease suffixes out of the URL.'
            echo '- The python CLI is consumed as a formula `resource` from the `fb_idb-*.tar.gz` sdist attached to this release, so update the `url` and `sha256` of that resource too.'
            echo '- If the tarball layout changed since the previous release, review the formula `install` block as well: a url/sha bump alone can ship a formula that fails to install or installs a companion that crashes at runtime.'
          } >> "$GITHUB_STEP_SUMMARY"

  python-client:
    name: Build fb-idb distributions
    runs-on: ubuntu-latest
    timeout-minutes: 20
    # See companion: run on dispatch (create-release skipped), block on failure.
    needs: [create-release]
    if: ${{ !cancelled() && (needs.create-release.result == 'success' || needs.create-release.result == 'skipped') }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Build sdist and wheel
        run: |
          pip install build
          # On workflow_dispatch the ref is a branch name, which is not a valid
          # PEP 440 version; use a placeholder so the build legs stay
          # exercisable without a tag.
          case "$GITHUB_REF" in
            refs/tags/v*) FB_IDB_VERSION="${GITHUB_REF_NAME#v}" ;;
            *) FB_IDB_VERSION="0.0.0.dev0" ;;
          esac
          FB_IDB_VERSION="$FB_IDB_VERSION" python -m build
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: fb-idb-dist
          path: dist/*
      - name: Attach to release
        if: startsWith(github.ref, 'refs/tags/v')
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "$GITHUB_REF_NAME" dist/* --clobber --repo "$GITHUB_REPOSITORY"
